# StockStone
StoneStock is a robust command-line interface (CLI) system designed to manage inventory for small and medium-sized businesses. The system must ensure local data persistence, high performance in memory concurrency, and surgical error handling.
